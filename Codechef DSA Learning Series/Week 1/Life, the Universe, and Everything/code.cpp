#include <bits/stdc++.h>
using namespace std;

int main()
{
    unsigned short int temp = 0;

    scanf("%u", &temp);
    while (temp != 42)
    {
        printf("%u\n", temp);
        scanf("%u", &temp);
    }
    return 0;
}